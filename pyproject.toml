[project]
name = "kannushi"
version = "0.1.0"
description = "A batch renderer for Jinja templates"
readme = "README.md"
authors = [
    { name = "Taras Denysenko", email = "terrapass93@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
kannushi = "kannushi:main"

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"
